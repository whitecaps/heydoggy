<!DOCTYPE html>
<html>
<head>
  <title>Heydoggy</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>


<meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <!-- Bootstrap core CSS -->
    <link href="http://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/3.0.2/css/bootstrap.css" rel="stylesheet" media="screen">

    
    <!-- HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
      <script src="http://cdnjs.cloudflare.com/ajax/libs/html5shiv/3.7/html5shiv.js"></script>
      <script src="http://cdnjs.cloudflare.com/ajax/libs/respond.js/1.3.0/respond.js"></script>
    <![endif]-->
    <style>
      html { height: 100% }
      body { height: 100%; background-color:#377ebe }
      #map-outer {height: 440px; padding: 20px; border: 2px solid #7ebe37; margin-bottom: 20px; margin-top: 30px; background-color: #FFF}
    #map-container { height: 400px}
    #map-container-index { height: 400px; width: 540px; margin-left: 45px}

    #registration-outer {height: 440px; padding: 20px; border: 2px solid #7ebe37; margin-bottom: 0px; margin-top: 30px; margin-left: 150px; margin-right: 150px; background-color: #FFF}
    #users-outer {height: 440px; padding: 20px; border: 2px solid #7ebe37; margin-bottom: 10px; margin-top: 30px; margin-left: 70px; margin-right: 70px; background-color: #FFF}
    </style>
    
</head>
<body>

<% if !request.original_url.include?("breed") %>
  <%= render "layouts/header" %>

<% if flash[:notice] %>
<p>
	<%= flash[:notice] %>
</p>
<% end %>

<%end%>

<%= yield %>

<% if !request.original_url.include?("breed") %>
<%= render "layouts/footer" %>
<% end %>
</body>
</html>
